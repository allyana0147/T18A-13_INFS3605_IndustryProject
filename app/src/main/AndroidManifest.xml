<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.infs3605_industry_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.INFS3605IndustryProject">
        <activity android:name=".UploadTextActivity"></activity>
        <activity android:name=".DictionaryDetailActivity" />
        <activity android:name=".ViewRewardsActivity" />
        <activity android:name=".RedeemRewardsActivity" />
        <activity android:name=".CommentsActivity" />
        <activity android:name=".UploadActivityVideo" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".DictionaryActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".NewPostActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>